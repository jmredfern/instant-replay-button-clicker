const assert = require('assert');
const buttonClicker = require('../index.js');

describe('buttonClicker', () => {
  it('has a test', () => {
    assert(false, 'buttonClicker should have a test');
  });
});
